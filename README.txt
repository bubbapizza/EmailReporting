Before doing anything, modify the config file under

./EmailReporting/appsettings.json


To compile this thing, run the following command:

    dotnet build

To run this thing, use the command:

    ./EmailReporting/bin/Release/net6.0/EmailReporting
